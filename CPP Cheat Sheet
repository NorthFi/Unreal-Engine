//// Print in Output Log ////
UE_LOG(LogTemp, Display, TEXT("This Message will be in your Output Log"));

//// Define variable and print it in Log ////
int NumberOfApples = 10;
int NumberOfOranges = 5;
float PriceOfApple = 0.99f;
float PriceOfOrange = 1.25f;
FString FruitName = TEXT("Apple");

// Print a int to the output log
UE_LOG(LogTemp, Display, TEXT("Number of apples: %d %d"), NumberOfApples, NumberOfOranges);

// Print a float to the output log
UE_LOG(LogTemp, Display, TEXT("\n Price per apple: $%f \nPrice per orange: $%f"), PriceOfApple, PriceOfOrange);

// Print a string to the output log
UE_LOG(LogTemp, Display, TEXT("\n Fruit Type: %s"), *FruitName);

//// Mathematics
int a = -10;
int b = -5;
int c = a * b;

UE_LOG(LogTemp, Display, TEXT("%d multiplied by %d is %d"), a, b, c)

float MyCalculation = 12.0f / 5.0f * 10.0f - 3.5f;

UE_LOG(LogTemp, Display, TEXT("MyCalculation is %f"), MyCalculation)

//// Variables in the header (Shows in the window)
// Editable in the window
	UPROPERTY(EditAnywhere)
	float PlatformSpeed = 20.0f;

// Showable in the window
	UPROPERTY(VisibleAnywhere)
	int PlatformHeight = 100;

//// Structs ////

// A struct is a group of varoables 
struct FVector
{
	float X;
	float Y;
	float Z;
};

// In header
	UPROPERTY(EditAnywhere)
	FVector MyVector = FVector(1102.0f, -1050.0f, 297.0f);

// In CPP
SetActorLocation(MyVector);

//// Game Loops ////
//All games use a game loop
//This is a loop that runs continuously and updates the game.
// 1. Get input
// 2. Update game state
// 3. Render graphics to the screen
MyVector.X = MyVector.X + PlatformSpeed;
SetActorLocation(MyVector);

	CurrentLocation.X = CurrentLocation.X + PlatformSpeed;
	SetActorLocation(CurrentLocation);

//// Struct Operations ////
FVector MyVector1 = FVector(1.0f, 2.0f, 3.0f);
FVector MyVector1 = FVector(4.0f, 5.0f, 6.0f);

FVector ResultVector = MyVector1 + MyVector2;
// ResultVector (5.0f, 7.0f, 9.0f)

//// Vector * Float ////
FVector MyVector = FVector(1.0f, 2.0f, 3.0f);
float SomeFloat = 10.0f;

FVector ResultVector = MyVector * SomeFloat;
// ResultVector (10.0f, 20.0f, 30.0f)


//// Functions //// 
void MyTestFunction()
{
	UE_LOG(LogTemp, Display, TEXT("This is a test function."));
}

MyTestFunction();

//// Function Parameters ////
void MyTestFunction(float Parameter1, int Parameter2)
{
	int MyInt = Parameter2 * 2;
	float Calculation = Parameter1 / 3.5f;
}
MyTestFunction(11.2f, 20);

//// Member Functions (In header file)////

class AMovingPlatform
{
	void MemberFunction(float MyParam);
}
//// Member Functions (In CPP file) ////
void AMovingPlatform::MemberFunction(float MyParam)
{
	FVector MyLocation = GetActorLocation();
	SetActorLocation(MyLocation * 2.0f);
}
